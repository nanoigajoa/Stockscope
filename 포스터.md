# StockScope — 포스터 원고

> **한 줄 정의**
> 기술적 분석 데이터를 자동으로 해석해, **종목의 건강도**와 **매수 타이밍**을 분리해서 제공하는
> 미국 주식 투자 의사결정 지원 플랫폼.

---

## Ⅰ. 아이디어

### 문제

개인 투자자의 기술적 분석은 두 개의 다른 질문을 하나로 섞어버린다.

- "이 종목이 **좋은 종목**인가?" (종목의 체력)
- "**지금** 사도 되는가?" (진입 타이밍)

좋은 종목이라도 고점에서 사면 손실이고, 타이밍이 좋아도 부실한 종목은 회복하지 못한다.
기존 도구는 이 둘을 하나의 점수로 합쳐 판단 근거를 흐린다.

### 해법

**두 질문을 두 개의 독립 서비스로 분리한다.**

| | 스크리닝 `/screen` | 매매시그널 `/signals` |
|---|---|---|
| 질문 | 기술적으로 건강한가? | 지금 진입 타이밍인가? |
| 축 | 종목의 **체력** | 진입의 **시점** |
| 출력 | S / A / B / SKIP 등급 | STRONG BUY / BUY / HOLD / NO SIGNAL |
| 관계 | 두 파이프라인 완전 독립 · **Watchlist로 연동** | |

> 스크리닝 카드의 ★을 누르면 Watchlist에 담기고, `/signals`가 그 종목의 타이밍을 계산한다.
> **"무엇을 살까"와 "언제 살까"를 순차적으로 답하는 구조.**

---

## Ⅱ. 구현 방법

### 1. Technical Screening — 기술적으로 건강한 종목 찾기

**Analysis (7개 체크리스트)**
- 이동평균 정배열
- RSI / MACD 추세
- 볼린저밴드 위치
- 거래량 변화
- 지지선 및 추세 지속성

**Output**
- ✓ **S / A / B / SKIP 등급 자동 분류** — 비율 기반(0.67 / 0.44 / 0.22)
  → 체크리스트 항목을 껐다 켜도 분모가 자동 조정돼 등급 기준이 유지된다
- ✓ **목표가 +8% / +15% · 손절 −7% 자동 계산**
- ✓ **RSI ≥ 80 하드게이트** — 점수가 높아도 무조건 SKIP (과열 추격매수 차단)
- ✓ 펀더멘털·외부 시그널 배지와 함께 종합 판단
  (실적발표 D−N · 숏비율 · 애널리스트 목표가 · 내부자 매수 · Google Trends · 의원 거래)

```
Finviz 사전 필터 → OHLCV 수집 → 뉴스 위험 필터
→ 지표 계산 → 7개 체크리스트 채점 → 등급 → 외부 시그널 4종 병렬
```

### 2. Trading Signal — 매수·매도 타이밍 분석

**Analysis — 3축 독립 가중 스코어링**

| 축 | 가중 | 무엇을 보는가 |
|---|:---:|---|
| ① **Entry Position** | 40% | 현재 가격이 적절한 매수 구간인지 (ATR 진입존 + 볼린저 %B 위치) |
| ② **Momentum Strength** | 35% | 상승 추세의 힘과 회복 가능성 (RSI 종형 곡선 + ADX 추세 강도) |
| ③ **Market Structure** | 25% | 기관 매집 구조와 수급 흐름 (Liquidity Sweep · FVG · POC · 캔들 패턴) |
| ＋ 수급 보너스 | +0.05 | CMF와 OBV가 **동시에** 자금 유입을 확인할 때만 가산 |

> **왜 3축을 독립으로 두는가**
> 한 축이 0이면 나머지가 높아도 진입 신호가 아니다.
> 세 축이 서로를 상쇄하지 않도록 각각 다른 정보 계층(가격 위치 / 가격 속도 / 자금 방향)에서 계산한다.

**Output**
- ✓ **STRONG BUY / BUY / HOLD / NO SIGNAL** 4등급 (≥0.60 / ≥0.40 / ≥0.20)
- ✓ **추천 진입 구간**(ATR 밴드) + **손절 기준** 제공
- ✓ **Confluence Check** — 구조 지지·모멘텀 회복·자금 유입 3개 독립 레이어 동시 충족 표시
- ✓ **EXIT 조건 감지** — 손절선 이탈 / RSI > 78 / MA 역배열 전환 / 진입존 극단 이탈

**점수를 신뢰하지 않는 안전장치 (등급 캡핑)**

| 트리거 | 조정 |
|---|---|
| SPY < MA50 (하락장) | STRONG BUY · BUY → **HOLD** |
| 실적발표 D−0~3 | STRONG BUY · BUY → **HOLD** |
| 실적발표 D−4~7 | STRONG BUY → **BUY** |
| MA20 < MA60 (역배열) | STRONG BUY 차단 |

> 개별 종목 신호가 맞더라도 **시장 조류와 이벤트 리스크에 역행하는 진입은 막는다.**

### 3. 웹 인터페이스

- **SSE 실시간 스트리밍** — 분석 진행 상황을 즉시 반영, 수십 종목 처리 중에도 화면이 멈추지 않음
  `EventSource → FastAPI generate() → ThreadPoolExecutor → done 이벤트(HTML)`
- **캔들 차트 모달** — Lightweight Charts 4.2, 매수 마커 6종 / 매도 마커 4종을 차트 위에 직접 표시
- **랭킹 보드** — Watchlist가 비어 있으면 Finviz 필터 결과를 시가총액 상위로 채워 자동 분석
- **사이드바 라이브** — SPY · VIX · 공포탐욕지수(15분 캐시) · FRED 매크로(7일 캐시)

### 4. Tech Stack

| 레이어 | 기술 |
|---|---|
| 백엔드 | FastAPI · Jinja2 · SSE · Python 3.12 |
| 데이터 | yfinance · Finviz(직접 스크래핑) · FRED · pytrends · quiverquant |
| 동시성 | `ThreadPoolExecutor` (종목별 병렬 수집·채점) |
| 캐시 | diskcache — TTL 다단계(5분 / 15분 / 24시간 / 7일) |
| 프론트 | Vanilla JS · Lightweight Charts 4.2 · TomSelect |
| 배포 | Render + GitHub Actions (keepalive · 일일 배치) |

---

## Ⅲ. Claude Code 활용법

### 핵심 원칙

> **"Design Document 없이는 코드를 생성하지 않는다."**
> 대화창은 휘발되지만 md 파일은 남는다. **문맥을 채팅이 아니라 파일에 저장한다.**

### 1. md 파일 계층 — 역할을 분리해 토큰을 아낀다

| 파일 | 역할 | 로딩 시점 |
|---|---|---|
| `CLAUDE.md` | **작업 규약** — 워크플로·금지사항·파일 로딩 전략 | 매 세션 자동 |
| `docs/design_doc.md` | 전체 시스템 설계 · 현재 상태 | 매 세션 |
| `docs/architecture.md` | 아키텍처 상세 · API 목록 | 구조 변경 시 |
| `docs/adr/` (19건) | **의사결정 기록** — 배경 / 결정 / 근거 | 관련 로직 수정 시 |
| `docs/skills/` (5건) | 재사용 구현 패턴 · Best Practices · **Anti-patterns** | 해당 레이어 작업 시 |
| `docs/phases/` | 실행 단위 태스크 + DoD | 현재 페이즈만 |
| `INDICATORS.md` | 도메인 지식 — 지표 계산식과 논리 | 지표 추가 시 |

핵심은 **"항상 로드"와 "필요할 때만 로드"를 파일 단위로 나눈 것.**
완료된 페이즈 문서는 다시 읽지 않는다.

### 2. 프롬프트 전략

**① 세션 시작 체크리스트로 문맥을 복원한다**
```
[ ] CASE 식별 (기존 / 신규)
[ ] Design Document 로드
[ ] 현재 페이즈 식별
[ ] 이 페이즈에 필요한 스킬만 로드
```

**② 코드로 탐색하지 않는다 — Planning Mode 우선**
"일단 만들어 보자"를 금지. 설계를 먼저 확정하고, 승인된 계획만 구현한다.

**③ 페이즈 단위로 자른다**
한 세션에 한 페이즈. DoD를 채우지 못하면 다음으로 넘어가지 않는다.
페이즈 종료 시 **변경 / 이유 / 리스크 / 다음 페이즈 의존성**을 기록한다.

**④ 결정은 ADR로 고정한다**
같은 논의를 세션마다 반복하지 않기 위해, 되돌릴 수 있는 판단을 문서에 못 박는다.

> **실제 사례 — ADR-019**
> 모멘텀 채점이 "과매도(StochRSI < 0.3)"를, 추세 채점이 "상승 추세(MA20 > MA60)"를 요구해
> **두 축이 동시에 켜질 수 없는 구조적 모순**을 발견.
> → 모멘텀 주 지표를 RSI 종형 곡선(45~65 최고점)으로 재설계하고 근거를 ADR로 남겼다.

**⑤ Anti-patterns를 자산으로 남긴다**
스킬 문서마다 "이렇게 하지 말 것"을 함께 적는다. 같은 함정을 두 번 밟지 않기 위해.

> **실제 사례 — Finviz 파서**
> Finviz가 티커 셀에 로고 이미지와 **폴백 이니셜**을 넣기 시작하자,
> 라이브러리가 텍스트를 평면화하며 전 컬럼을 한 칸씩 밀어버렸다.
> 에러 없이 `Ticker = "C"`, 가격 칸에 P/E가 들어오는 **조용한 오염**.
> → 텍스트 대신 `data-*` 속성을 읽도록 직접 파서를 작성하고,
> 원인·재현·검증법을 스킬 문서의 Anti-patterns에 기록했다.

### 3. 결과

```
19개 ADR · 5개 스킬 문서 · 3개 페이즈 문서 · 2,500+ 줄의 설계 문서
```
새 세션이 시작될 때마다 아키텍처를 다시 설명하지 않는다.
**문서가 곧 컨텍스트이고, 컨텍스트가 곧 코드 품질이다.**

---

## 포스터 레이아웃 제안

```
┌───────────────────────────────────────────────┐
│  StockScope  |  한 줄 정의                     │  ← 헤더
├───────────────────┬───────────────────────────┤
│ Ⅰ. 아이디어        │  Ⅱ. 구현 방법             │
│  · 문제(두 질문)   │   ① Screening  (스크린샷) │
│  · 해법(서비스분리)│   ② Signal 3축 (스크린샷) │
│  · 연동 다이어그램 │   ③ 캡핑 · SSE            │
├───────────────────┴───────────────────────────┤
│ Ⅲ. Claude Code 활용법                          │
│  md 계층 표  |  프롬프트 5원칙  |  사례 2건    │
├───────────────────────────────────────────────┤
│  Tech Stack 배지 나열                          │  ← 푸터
└───────────────────────────────────────────────┘
```

**시각 자료 체크리스트**
- [ ] `/screen` 카드 스크린샷 (S등급 카드 + 배지가 많이 붙은 것)
- [ ] `/signals` 카드 스크린샷 (3축 스코어 바 + Confluence + EXIT 배지)
- [ ] 캔들 차트 모달 (매수/매도 마커가 함께 찍힌 구간)
- [ ] 파이프라인 다이어그램 (Finviz → … → 등급)
- [ ] 3축 가중 도넛/바 차트 (40 / 35 / 25)
- [ ] md 파일 계층 트리 이미지
