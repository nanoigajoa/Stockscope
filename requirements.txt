# 코어
yfinance>=0.2.0
pandas>=2.0.0
numpy>=1.24.0,<2.3.0
schedule>=1.2.0
colorama>=0.4.6
requests>=2.31.0
diskcache>=5.6.0
# finviz_filter.py는 requests+lxml로 Finviz 스크리너를 직접 파싱한다.
# (finviz 라이브러리는 티커 셀의 로고 이니셜 때문에 컬럼이 밀려 제거)
lxml>=5.0.0
cssselect>=1.2.0        # lxml .cssselect() 호출에 필요 (lxml 기본 포함 아님)

# 웹앱
fastapi>=0.109.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0
watchfiles>=1.0.0
jinja2>=3.1.0
python-dotenv>=1.0.0

# 외부 데이터소스
pytrends>=4.9.0
quiverquant>=0.1.0
sec-edgar-downloader>=0.18.0
# macro_fetcher.py는 requests로 FRED REST API 직접 호출 (fredapi 미사용)

# Testing & Indicators
pandas-ta>=0.3.14b0
pytest>=8.0.0
