# StockScope 개발 일지

개인 프로젝트로서 StockScope를 설계·구축한 과정을 기술적·기획적 관점에서 정리한 문서.

---

## 1. 문제 정의

### 개인적 동기

미국 주식 개인 투자를 하다 보면 두 가지 질문을 반복하게 된다.

- 이 종목, 기술적으로 지금 건강한가?
- 관심 두고 있는 종목, 오늘이 들어갈 타이밍인가?

### 해결하려 한 문제

관심 종목 20개를 하나씩 차트를 열고 확인하면 종목당 5~10분, 전체 1~2시간이 걸린다. 더 큰 문제는 "감"으로 판단한다는 것 — 기준이 일관되지 않고, 어제와 오늘 같은 차트를 보고 다른 결론을 낸다.

**목표:** 내 판단 기준을 코드로 정형화하고, 20개 종목의 기술적 분석 결과를 수초 내에 일관된 스코어로 뽑아보는 도구.

결과적으로 구현된 시스템:
- 스크리닝: Finviz 사전 필터 → 7개 체크리스트 자동 채점 → S/A/B/SKIP 등급
- 매매시그널: Watchlist 종목에 대해 3축 독립 스코어링 → STRONG BUY / BUY / WATCH / NO SIGNAL
- SSE 스트리밍: 분석 중 상태를 실시간으로 화면에 표시 → 체감 대기 시간 제거

---

## 2. 프로젝트 타임라인

| 시기 | 주요 결정 |
|------|----------|
| 2026-04-24 | FastAPI + SSE로 웹앱 전환 결정 |
| 2026-04-26 | 커스터마이징 패널 (기간 / RSI 범위 / 체크리스트 on-off) |
| 2026-04-27 | 펀더멘털 배지 4종 + About 페이지 |
| 2026-04-28 | FRED · Google Trends · 의원거래 · 내부자 거래 통합 |
| 2026-04-29 | 매매시그널 분리 — 두 번째 독립 서비스 |
| 2026-04-30 | Lightweight Charts 모달 + 매수/매도 마커 |
| 2026-05-01 | 시그널 스코어링 QA — 지표 이진화 제거, 연속값으로 전환 |
| 2026-05-01 | 스크리닝 ↔ 시그널 서비스 완전 분리 (Phase 0~6) |
| 2026-05-02 | 차트 마커 아키텍처 분리 · RSI 종형 모멘텀 · CMF 도입 |
| 2026-05-07 | 전문가 리뷰 반영 → FVG · ADX · POC · Chandelier Exit 추가 |
| 2026-05-08 | 3축 독립 스코어링 전면 재설계 |
| 2026-05-09 | Confluence Check 구현 |

---

## 3. 핵심 기술 의사결정 (Engineer's Decisions)

### 3-1. SSE 채택 — 오버엔지니어링 경계

**상황:** 종목 분석에 20~40초가 걸린다. 브라우저에서 이 과정을 실시간으로 보여주려면 스트리밍이 필요하다.

**초기 접근:** Celery + Redis 비동기 큐. 아키텍처적으로 "맞는" 선택이지만, Redis 데몬 관리 · Docker 없는 설치 · 재시작 시 큐 복구 — 혼자 유지보수하는 개인 프로젝트에선 운영 비용이 지나쳤다.

**결정: SSE(Server-Sent Events) + ThreadPoolExecutor.**

```
브라우저 EventSource → FastAPI generate() → ThreadPoolExecutor → done 이벤트(HTML)
```

외부 프로세스 없이 FastAPI 단일 서버로 실시간 스트리밍 구현. 분석이 완료되면 렌더링된 HTML 전체를 `done` 이벤트로 전송, 클라이언트는 `innerHTML` 교체만 담당. 프론트 복잡도를 최소화하면서 원하는 UX를 얻었다.

**교훈:** "기술적으로 올바른 선택"이 항상 "지금 상황에서 올바른 선택"은 아니다. 규모와 운영 주체에 맞는 스택을 고르는 것 자체가 엔지니어링 판단이다.

### 3-2. 스크리닝 vs 시그널 서비스 분리

**초기 실수:** 스크리닝과 시그널을 하나의 파이프라인으로 묶었다.

**문제:**
- 시그널만 보려 해도 스크리닝 전체(Finviz 필터 → 7개 체크리스트)를 실행해야 했다
- Watchlist에 이미 고른 종목은 스크리닝이 필요 없는데 강제로 거쳐야 했다
- 스크리닝 SKIP 판정 종목은 시그널 채점 자체가 불가능했다

**결정:** 두 서비스를 완전히 독립된 파이프라인으로 분리.

```
스크리닝:  finviz_filter → fetch_ohlcv → checklist → grade
시그널:    watchlist_store → fetch_ohlcv → score_signals
```

연결 고리는 스크리닝 카드의 ★ 버튼 → `POST /api/watchlist/{ticker}` → `/signals` 자동 분석. 두 서비스는 독립이지만 사용자 흐름은 자연스럽게 이어진다.

### 3-3. 이진 채점 → 연속값 정규화

**상황:** 초기 스코어링은 `if StochRSI < 0.3: score += 1` 방식이었다.

**문제:** 점수가 0 아니면 1이어서 경계값(0.31)에서 억울하게 0점이 되고, 0.01과 0.29가 동일하게 취급됐다. 결과적으로 STRONG BUY가 거의 발생하지 않았다.

**결정:** 모든 지표를 0.0~1.0 연속값으로 정규화.

```python
# 이전 (이진)
stoch_ok = k < 0.3   # 0 아니면 1

# 이후 (연속값)
stoch_bonus = min((k - k_prev) / 0.05, 0.3) if k > k_prev and k_prev < 0.35 else 0
```

부드러운 점수 분포가 생기고, 경계값 근처의 왜곡이 사라졌다.

### 3-4. venv를 iCloud 밖으로 이전 (환경 문제)

macOS iCloud Drive가 Desktop을 동기화하면서 `.venv` 파일을 evict(로컬 삭제)하는 현상이 발생. 서버 시작 중 `fastapi/__init__.py`를 찾지 못하는 오류.

**결정:** venv 실체를 `~/fin_auto_venv`로 이전, `fin_auto/.venv`는 심링크. `run.sh`에서 `~/fin_auto_venv/bin/python`을 직접 호출.

---

## 4. 시행착오 — 틀렸다가 고친 것들

개발에서 가장 많이 배운 건 잘 된 결정이 아니라 완전히 틀린 결정들이었다.

### 4-1. Trend와 Momentum이 서로를 죽이는 구조

**설계 의도:** MA 정배열(상승 추세) + StochRSI 과매도(눌림목) = 최고의 진입 타이밍.

**실제 결과:** 수학적으로 동시 달성이 불가능했다.
- MA 정배열 = 가격이 MA20 위에 있다 = 최근 상승
- StochRSI 과매도 = 최근 급락 중

두 조건이 모순이었다. Trend=1.0이면 Momentum≈0, Momentum=1.0이면 Trend≈0. STRONG BUY 기준(0.70)을 어떻게 설정해도 달성이 거의 불가능했다.

**수정:** 모멘텀 기준을 "과매도 탈출"에서 "RSI 45~65 건강한 상승 구간"으로 전환. 상승 추세 속 눌림목(RSI가 55로 수렴 중)이 실제로 좋은 진입 타이밍이라는 도메인 이해를 반영했다.

### 4-2. "지표 더 많이 = 신뢰도 더 높음" 오류

**설계 의도:** OBV + CMF + MFI를 함께 쓰면 수급 신호가 더 정확해진다.

**실제 결과:** 세 지표가 모두 "거래량 × 가격 방향"을 측정하는 다른 이름이었다. Volume 카테고리 가중치가 실질적으로 과대 집계됐다. STRONG BUY가 나오는 조건이 "거래량 관련 지표 3개가 동시에 켜질 때"가 됐는데, 이는 같은 정보를 반복 계산한 결과였다.

**수정:** Volume 카테고리를 독립 축에서 "교차 검증 보너스(+5%)"로 강등. CMF + OBV 이중 확인 시만 가산. 진짜 독립 정보인 Structure(가격 구조) 카테고리를 신설해 그 자리를 채웠다.

### 4-3. 매수 마커 아키텍처의 상태 머신 문제

**설계 의도:** 매수/매도 신호를 하나의 상태 머신으로 관리. 포지션이 없을 때만 매수, 있을 때만 매도를 표시.

**실제 결과:** 데드크로스(MA 역배열) 구간 내내 매도 마커가 연속으로 찍혔다. 마커가 차트를 도배해서 의미를 잃었다.

**수정:** 매수/매도를 별도 파일(`buy_signal.py`, `sell_signal.py`)로 분리. 매도는 "전환 이벤트"(데드크로스 당일, RSI 고점 하락 시작일)에만 찍히도록 변경. 상태 지속이 아닌 상태 변화에 반응하는 구조.

---

## 5. 시그널 스코어링의 진화

### v1: 카테고리 4개, 이진 지표 (실패)

```
Trend(35%) + Momentum(25%) + Volume(25%) + Pattern(15%)
각 카테고리: bool 조건 2개의 평균
```

문제: [4-1], [4-2] 참고. STRONG BUY 달성 불가, 지표 간 모순 구조.

### v2: 연속값 + RSI 종형 모멘텀

모든 지표를 0.0~1.0 연속값으로 정규화. 모멘텀 기준을 RSI 45~65 상승 구간으로 전환. 스코어 분포가 정상화됐고, STRONG BUY가 실제로 발생하기 시작했다.

### v3: 3축 독립 구조 (현재)

전문가 리뷰에서 핵심 지적: "CMF·OBV·MFI가 모두 같은 걸 측정하고 있다."

**재설계 원칙: 서로 다른 정보 계층이 겹칠 때만 신뢰도 높은 신호다.**

```
축 1 - 진입 포지션(40%): 지금 가격이 좋은 위치인가?   → 가격 위치
축 2 - 모멘텀 강도(35%): 반등 속도와 추세 강도는?      → 가격 속도
축 3 - 구조 확인(25%):   기관 흔적이 지지하는가?        → 가격 구조
보너스 - 수급(+5%):      CMF·OBV 이중 확인 (교차 검증)
```

핵심: STRONG BUY 달성은 구조 확인(FVG·POC·LiquiditySweep) 없이 수학적으로 거의 불가능하도록 설계했다. 점수를 높이는 게 목적이 아니라, "어떤 신호를 신뢰할 수 있는가"를 설계에 담은 것이다.

---

## 6. Confluence Check — 제품 철학으로서의 신뢰도 설계

3축 스코어링 이후 한 가지 질문이 남았다: **STRONG BUY 신호가 나왔을 때, 이게 진짜인지 어떻게 알 수 있는가?**

점수를 더 높이는 것(등급 공식 변경)은 백테스트 없이 검증 불가능한 가정이다. 대신 "신뢰도를 별도 레이어로 표시"하는 방향을 선택했다.

**핵심 아이디어:** 단순히 점수가 높은 종목이 아니라, 서로 독립적인 데이터 소스 — 가격 구조(차트에 남겨진 기관 흔적), 가격 속도(RSI 회복 궤적), 자금 방향(CMF) — 가 동시에 같은 결론을 가리킬 때만 최고 신뢰도로 분류한다.

| 레이어 | 측정 대상 | 독립성 근거 |
|--------|----------|------------|
| 구조 지지 (FVG / POC) | 기관이 지킨 가격대 | 보조지표와 무관한 차트 구조 |
| 모멘텀 회복 (RSI 35~60) | 과매도 후 회복 확인 | 가격 속도, 거래량과 독립 |
| 자금 유입 (CMF > 0) | 실제 자금 방향 | 거래량×가격 합성, 순수 자금 방향 |

3/3 동시 충족 = "가격 구조·속도·자금이 모두 같은 방향이다" → 최고 신뢰도.

UX 설계도 이 철학을 반영했다. 카드에는 작은 점 3개(● ○ ○)로 조용히 표시하고, 3/3 달성 시에만 눈에 띄는 `CONFLUENCE` 배지로 업그레이드. 0/3이면 아무것도 표시하지 않는다(노이즈 방지). 모달에서는 각 레이어의 실제 수치(RSI 56, CMF +0.08)와 통과/실패 이유를 함께 보여준다.

---

## 7. 기술 스택 선택 이유

### FastAPI + Jinja2 (HTMX 없이)

HTMX를 검토했지만 SSE 스트리밍과 조합하면 오히려 복잡해졌다. 결과 HTML을 서버에서 완전히 렌더링해서 `innerHTML`로 교체하는 방식이 더 직관적이었다.

### Vanilla JS (React/Vue 없이)

카드 렌더링이 전부 서버사이드(Jinja2)라 클라이언트 상태 관리가 거의 필요 없다. SSE 핸들러 + 모달 로직 + 필터 정도면 Vanilla JS 파일 1개로 충분. 불필요한 번들러와 빌드 파이프라인을 없앴다.

### yfinance (유료 API 없이)

15~20분 지연이지만 개인 스크리닝 목적에서는 충분하다. API 키 불필요, 무료, OHLCV + 펀더멘털 + 실적일까지 제공. 유료 API는 백테스팅 엔진이 필요해질 때 고려할 것이다.

### diskcache (Redis 없이)

멀티 레이어 TTL 캐시(5분~7일)가 필요했지만 Redis는 별도 프로세스 관리가 부담이었다. diskcache는 파일 시스템 기반 순수 Python 라이브러리로 동일한 TTL 기능을 제공하고, 서버 재시작 후에도 캐시가 유지된다. 캐시 적중 시 동일 세션 내 API 재요청이 없어 외부 API 레이트 리밋 문제도 사라졌다.

### Render 무료 플랜 (운영비 0원)

15분 비활성 슬립 문제는 GitHub Actions keepalive 워크플로우(10분 간격 ping)로 해결. 일일 배치는 KST 07:00 cron으로 강제 갱신.

---

## 8. 전문가 리뷰를 통한 모델 검증 및 아키텍처 고도화

퀀트 엔지니어링 관점 코드 리뷰 한 번이 아키텍처를 두 단계 진화시켰다.

**리뷰 핵심 지적:**
> "OBV·CMF·MFI는 모두 거래량 기반 수급 방향을 측정한다. 현재 시스템은 같은 정보를 세 번 계산하면서 Volume 카테고리에 과도한 가중치를 부여하고 있다. 진짜 독립 신호는 가격 구조(FVG), 가격 속도(RSI), 자금 방향(CMF) 세 축이다."

**비판적 수용:** 지적을 받기 전까지는 "지표가 많을수록 신뢰도가 올라간다"는 암묵적 가정을 갖고 있었다. 리뷰를 통해 독립성 없는 지표의 합산이 오히려 특정 정보 계층에 가중치를 몰아주는 설계 결함임을 이해했다.

**구조적 개선:**
1. 상관 지표 중복 제거: Volume 카테고리 → 교차 검증 보너스로 강등
2. 독립 축 신설: Structure 카테고리(FVG·POC·LiquiditySweep) — 가격축 기반
3. 신규 지표 이식: FVG(SMC 기관 갭 구조), Volume Profile POC(기관 평단가), ADX(추세 강도), Chandelier Exit(추세 이탈 매도)

단순히 지표를 추가하는 작업이 아니라, "어떤 정보 계층을 측정하는가"를 먼저 정의하고 그에 맞는 지표를 선택하는 설계 방식으로의 전환이었다.

---

## 9. 아직 안 된 것들

| 항목 | 이유 |
|------|------|
| 백테스팅 | 과거 시그널 재현 → 승률/MDD 계산. 현재 지표 조합의 유효성을 검증하는 데 필수지만, 설계가 안정된 뒤 의미가 생긴다 |
| AVWAP | 앵커 기준점 자동 선택 알고리즘이 복잡. 수동 앵커 UI가 먼저 필요 |
| 텔레그램 알림 | STRONG BUY 발생 시 push 알림. batch_scheduler 통합 예정 |
| 실시간 데이터 | yfinance 15분 지연 한계. 유료 API 또는 WebSocket 기반 전환 필요 |

---

## 10. 코드로 보는 설계 철학

**명시적 실패보다 조용한 성공을 우선한다.**
지표 계산이 실패하면 0점 처리하고 다음 지표로 넘어간다. 이건 의도적이다 — 데이터 품질 문제 하나가 전체 분석을 죽이면 안 된다.

**하나의 결과를 여러 곳에서 계산하지 않는다.**
모든 지표 계산은 `screener/indicators.py`에서, 채점 로직은 `screener/signal_scorer.py`에서만. 라우트 파일은 계산하지 않고 렌더링만 한다.

**캐시는 TTL로 설계한다.**
데이터 특성에 맞는 TTL: 주가 당일 캐시, 실시간 가격 5분, FRED 매크로 7일. 만료 시 자동 갱신, 별도의 캐시 무효화 로직 없음.

**백엔드에서 HTML을 완전히 렌더링한다.**
클라이언트는 JSON API를 해석하지 않는다. 서버가 Jinja2로 HTML을 만들어서 SSE `done` 이벤트로 전송, 클라이언트는 `innerHTML` 교체만. 프론트엔드 복잡도가 대폭 줄어든다.
